{
  "name": "Homie — AI ISA for Royal LePage",
  "description": "AI Inside Sales Agent that qualifies real estate leads and books appointments via GoHighLevel CRM and ElevenLabs voice AI. Replaces Verse.ai at a fraction of the cost.",
  "model": "gpt-4o",
  "tools": [
    {
      "type": "mcp",
      "server": "gohighlevel-mcp",
      "env": {
        "GHL_API_KEY": "${GHL_API_KEY}",
        "GHL_LOCATION_ID": "${GHL_LOCATION_ID}"
      }
    },
    {
      "type": "mcp",
      "server": "elevenlabs-mcp",
      "env": {
        "ELEVENLABS_API_KEY": "${ELEVENLABS_API_KEY}"
      }
    }
  ]
}
